import { styled } from '@styles'

export const Select = styled('select', {
  width: '100%',
  padding: '$1 $2',
  border: '1px solid $gray400',
  borderRadius: '$sm',
  color: '$gray900',
  fontSize: '$sm',
})
